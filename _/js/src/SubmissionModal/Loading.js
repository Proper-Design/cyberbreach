import React from "react";

const Loading = () => {
  return (
    <div className="modal-message-wrapper">
      <div className="modal-message">Sending...</div>
    </div>
  );
};

export default Loading;
